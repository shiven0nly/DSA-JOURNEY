// arrays: are mostly use to store the data when large set is given..
// linear collection of same type of elements that are stored together in contigous memory spaces.

#include <iostream>
using namespace std;

int main(){

    int arr[] = {12,24321,53,12312};
    char arr2[] = {'A', 'a', '23'};
    // arrays ke index ka mtlb= position, it starts with 0th position, bhout saare log , ek line me khade hai toh , woh konse number per khade unko hum array bolte hai;
    // size of arr = (no. of elements) * (size of variable type {int, char, float, double})
    return 0;
}